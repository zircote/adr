# Dependabot Auto-Merge
# Automatically approves and merges Dependabot PRs after CI passes
#
# This workflow calls the centralized auto-merge workflow from zircote/.github
# Requires repository setting "Allow auto-merge" to be enabled

name: Dependabot Auto-Merge

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    # yamllint disable-line rule:line-length
    uses: zircote/.github/.github/workflows/reusable-dependabot-automerge.yml@574eb0a971bbce5e37fe9949ed62a9a95e0f9133  # main
    secrets: inherit
